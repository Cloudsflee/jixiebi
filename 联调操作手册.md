# 机械臂联调操作手册（CH340 + Web 数字孪生）

## 1. 适用范围

本文档适用于当前项目的最小可用联调链路：

- 硬件链路：`PC -> CH340(COM5) -> STM32 控制板 -> 总线舵机`
- 软件链路（控制闭环）：`front/web -> WebSocket -> front/gateway -> 串口 -> STM32`
- 教学演示链路（可视化）：`teach_front/web`（本地三维/IK/伪 FEA，不直接下发串口）
- 当前串口文本协议：`M / Q / V / T / PING`（USART1）

---

## 2. 目录与角色

- `front/gateway`：Node.js 串口网关（WebSocket <-> CH340）
- `front/web`：实机联调前端（可连接网关并控制舵机）
- `teach_front/web`：教学展示前端（关节示教、IK、伪 FEA）
- `SuArmT`：主模型源目录（URDF / meshes / launch / config）

当前结构同步策略：

- 两个前端都以 `SuArmT` 作为统一结构源
- 通过脚本自动同步到：
  - `front/web/suarmt/`
  - `teach_front/web/suarmt/`

---

## 3. 联调前准备

### 3.1 硬件确认

1. CH340 串口已识别（当前示例：`COM5`）。
2. 总线舵机接线正确，至少 `ID=1` 在线。
3. 舵机电源稳定（建议先确认电压，避免欠压报警）。
4. 跳帽状态正确：
   - 烧录时切到烧录态。
   - 运行时切回运行态。

### 3.2 软件确认

1. 可用 `make` 与 `arm-none-eabi-gcc`。
2. 可用 `STM32_Programmer_CLI`。
3. 可用 Node.js（Windows 建议 `cmd /c npm ...`）。
4. 可用 Python（用于静态页面服务）。

---

## 4. 一次完整联调流程

### 4.1 编译固件

在项目根目录执行：

```bash
make -f gcc/Makefile clean all
```

产物：

- `gcc/build/OpenArmSTM32_gcc.hex`
- `gcc/build/OpenArmSTM32_gcc.bin`

### 4.2 烧录固件

```bash
make -f gcc/Makefile flash FLASH_PORT=COM5 FLASH_BAUD=115200
```

完成后：

1. 按一次 `RST`。
2. 切回运行态。

### 4.3 启动串口网关（front/gateway）

首次安装依赖：

```bash
cd front/gateway
cmd /c npm install
```

启动网关：

```bash
cmd /c npm start
```

默认参数：

- 串口：`COM5`
- 波特率：`9600`
- WebSocket：`ws://127.0.0.1:8787`

成功日志应包含：

- `Gateway listening on ws://127.0.0.1:8787`
- `Serial opened: COM5 @ 9600`

### 4.4 启动控制前端（front/web）

建议以 `front/` 作为静态服务根目录：

```bash
cd front
python -m http.server 8090
```

浏览器访问：

- `http://127.0.0.1:8090/web/`

### 4.5 启动教学前端（teach_front/web）

```bash
python -m http.server 8091 --bind 0.0.0.0 --directory "d:\04_projects\国创-数字孪生机械臂\STM32\teach_front"
```

浏览器访问：

- `http://127.0.0.1:8091/web/index.html`

说明：

- `teach_front/web` 当前不通过网关下发串口指令。
- 其主动功能是教学演示交互（关节滑块、IK、伪 FEA、课程步骤）。

#### 03 有限元工况输入（简化结构力学模型）

进入「03 有限元演示」后，**工况输入**区包含：

| 输入项 | 作用 |
| --- | --- |
| 姿态来源 | L1–L4 课时姿态，或 **当前孪生姿态**（与 01/02 滑条联动） |
| 载荷工况 | 轻载 20% / 额定 55% / 极限 86%（可再用滑条细调） |
| 载荷方向 | 重力主导 / 侧向 / 组合（影响弯矩估算） |
| 材料预设 | 6061 铝 / 轻质对比（屈服强度与截面） |
| 变形放大 | **仅影响云图可见度**，不参与力学计算 |

侧栏「一键教学演示」在 03 阶段会按 `joints.json` 中 `feaLabCases` 自动播放完整工况；演示结束后可手动改参数并点「运行有限元演示」。

02 面板内「同步到实机 / 从实机读取」需网关连通（见 4.4），与有限元分析独立。

### 4.6 front 页面联调步骤（建议顺序）

1. 点击“连接”，状态变为已连接。
2. 点击 `PING`，日志出现 `OK`。
3. 先测 `J1(ID=1)`：
   - 位置先用 `500`
   - 时间先用 `300ms`
   - 发送后观察舵机动作
4. 读取位置：应回 `P,1,<pos>`。
5. 读取电压：应回 `V,1,<mv>`。
6. 读取温度：应回 `T,1,<temp_c>`。
7. 单轴稳定后再扩展到 `ID=2~6`。

---

## 5. 模型结构同步与重建（SuArmT）

当 `SuArmT` 模型更新后，在项目根目录依次执行：

```bash
node front/web/tools/sync_suarmt_structure.js
node front/web/tools/rebuild_model_from_suarmt.js
node front/web/tools/generate_urdf_from_joints.js
```

脚本作用：

1. `sync_suarmt_structure.js`
   - 同步 `SuArmT` 原始结构到：
     - `front/web/suarmt/`
     - `teach_front/web/suarmt/`
2. `rebuild_model_from_suarmt.js`
   - 解析 `SuArmT.urdf`
   - 生成运行 STL：
     - `front/web/raw/suarmt_global_*.STL`
     - `teach_front/web/models/raw/suarmt_global_*.STL`
   - 重写：
     - `front/web/joints.json`
     - `teach_front/web/joints.json`
3. `generate_urdf_from_joints.js`
   - 从 `front/web/joints.json` 生成运行态 `arm_from_joints.urdf`

---

## 6. 协议速查

### 6.1 下发（gateway -> STM32）

- `PING`
- `M,<id>,<pos>,<time_ms>`
- `Q,<id>`
- `V,<id>`
- `T,<id>`

### 6.2 回包（STM32 -> gateway）

- `OK`
- `ERR,<code>`
- `P,<id>,<pos>`
- `V,<id>,<mv>`
- `T,<id>,<temp_c>`

参数建议：

- `id`: `1~253`
- `pos`: `0~1000`
- `time_ms`: `20~30000`

---

## 7. 常见问题与处理

### 7.1 `COM5` 打不开 / 被占用

处理：

1. 关闭串口助手、IDE 串口监视器、烧录工具。
2. 重新插拔 CH340。
3. 重启网关或重试烧录。

### 7.2 front 显示已连接但舵机不动

处理顺序：

1. 检查网关日志是否收到 `move` 指令。
2. 确认在线舵机 ID 与页面发送 ID 一致（先用 `ID=1`）。
3. 确认开发板在运行态。
4. 确认舵机供电正常。

### 7.3 页面提示 Three.js 依赖未加载

处理：

1. 强制刷新缓存（`Ctrl+F5`）。
2. 确认可访问：
   - `http://127.0.0.1:8090/web/vendor/three/three.module.js`
3. 关闭旧标签页后重新打开。

### 7.4 teach_front 页面空白或模型不显示

处理：

1. 检查访问路径是否为：
   - `http://127.0.0.1:8091/web/index.html`
2. 确认模型文件存在：
   - `teach_front/web/models/raw/suarmt_global_*.STL`
3. 执行一次模型重建：
   - `node front/web/tools/rebuild_model_from_suarmt.js`
4. 强制刷新浏览器缓存（`Ctrl+F5`）。

### 7.5 烧录成功但程序未运行

处理：

1. 切回运行态。
2. 按 `RST`。
3. 再观察网关与页面是否恢复联调。

---

## 8. 联调验收清单

满足以下条件可判定“基础联调通过”：

1. 固件可稳定编译与烧录。
2. 网关可稳定打开 `COM5`。
3. `front/web` 可连接网关并收发状态。
4. `ID=1` 可执行运动并回读位置。
5. 可回读电压，系统无异常报警。
6. `teach_front/web` 可正常加载 `SuArmT` 重建模型并进行交互演示。

---

## 9. 下一步建议

1. 补齐 `J1~J5` 舵机 ID 映射与标定记录（`J6/J7` 跟随 `J5`）。
2. 固化角度与 `0~1000` 的映射参数（含零点、限位、反向）。
3. 对 `teach_front` 与 `front` 的同姿态显示结果做一次逐关节对齐检查。

---

## 10. SuArmT 结构语义基线（front + teach_front 同步）

两套前端均以 `SuArmT/urdf/robot_slider_control_level.m` 作为结构与控制语义基线，重点如下：

1. 关节实体总数为 `7`（`J1~J7`），部件实体总数为 `8`（含 `base`）。
2. 可见主动控制点为 `4` 个：`J1 / J2 / J3 / J5`。
3. `J4` 是联动派生关节，不作为独立主动滑块：
   - `q4 = q4_offset - q2 - q3`
4. 夹爪联动：
   - `J6 = J5`
   - `J7 = J5`
5. 关节限位统一来源：
   - 使用 `SuArmT/urdf/robot_slider_control_level.m` 中 `jointMins/jointMaxs`
   - `front` 与 `teach_front` 重建后保持同一限位，不再沿用旧 JSON 历史值
6. 结构树关系固定：
   - `base -> J1 -> J2 -> J3 -> J4`
   - `J4 -> J5`
   - `J4 -> J6`
   - `J4 -> J7`

如果页面出现“控制点数量异常/底座歪/缺件”，优先检查这 6 条是否被破坏。

### 10.1 front 结构检查

在 `front/web` 页面启动后，日志应可见：

- `Assembly lock mode enabled` 且 `disableCouplings=true`
- `structure snapshot` 中 `jointCount=7`
- `mesh load summary` 中每个 `target` 均有加载计数

同时确认：

1. `front/web/joints.json` 中：
   - `j4.derivedType = "offset_minus_sum"`
   - `j4.uiHidden = true`
   - `j6/j7.derivedType = "follow"`
   - `j6/j7.derivedSourceTarget = "j5"`
2. `front/web/raw` 目录存在：
   - `suarmt_global_base_link.STL`
   - `suarmt_global_01.STL` ... `suarmt_global_07.STL`
3. 页面可见控制卡数量应为 `4`（`J1/J2/J3/J5`）。
4. `front/web/joints.json` 中 `frameCalibration` 应存在，且推荐：
   - `mode = "fixed_j1_front"`
   - `upTarget = "j1"`
   - `frontTarget = "j4"`
   - `frontAxis = "x"`
   - `useDynamicFallback = false`

### 10.2 teach_front 结构检查

1. `teach_front/web/joints.json` 中语义同上：
   - `j4` 为 `offset_minus_sum` 且隐藏
   - `j6/j7` 跟随 `j5` 且隐藏
2. `teach_front/web/models/raw` 目录存在 `suarmt_global_*.STL` 全量文件。
3. 教学页关节滑块数量应为 `4`（`J1/J2/J3/J5`）。
4. `teach_front/web/joints.json` 中 `frameCalibration` 与 `front` 保持一致。

### 10.3 一键重建命令（推荐）

在项目根目录执行：

```bash
node front/web/tools/rebuild_model_from_suarmt.js
```

该命令会同时重写：

- `front/web/joints.json`
- `teach_front/web/joints.json`
- `front/web/raw/suarmt_global_*.STL`
- `teach_front/web/models/raw/suarmt_global_*.STL`

重建后请强制刷新浏览器缓存（`Ctrl+F5`）。
