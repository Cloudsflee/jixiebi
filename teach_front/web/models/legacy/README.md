# Legacy Model Assets

This folder stores historical STL assets that are not part of the current SuArmT global export pipeline.

- Active runtime assets remain in `../raw/` and are referenced by `teach_front/web/joints.json`.
- Files in this folder are kept only for traceability and fallback comparison.
