# 机械臂数字孪生方案（基于当前真实进展）

## 1. 目标与边界

- 目标：实现本地数字孪生 MVP。
- 要求：Web 端可控制机械臂运动，且 Web 仿真与实机动作同步。
- 机械臂：4 个总线舵机（当前已确认在线舵机 ID=1）。
- 通信链路：PC 通过 CH340 串口连接开发板（当前常用 COM5）。
- 当前阶段不纳入：蓝牙控制、云端部署、ROS 体系。

## 2. 当前真实状态（已完成）

- 固件工程已切换为 GCC + Makefile，编译和串口烧录可重复通过。
- 已完成总线舵机底层重构：
  - 半双工方向脚：PA0/PA1（按当前示例真值表）
  - 协议帧：0x55 0x55（舵机原生协议）
  - 已支持接口：
    - `BusServoMove`
    - `BusServoReadID`
    - `BusServoReadPosition`
    - `BusServoReadVin`
    - `BusServoSetID`
    - `BusServoLoadOrUnload`
- ID=1 实机已验证可运动。
- 已在 `USART1` 增加文本协议（与原 `0x55` 二进制协议并存）：
  - 下发：`M/Q/V/PING`
  - 回包：`OK/ERR/P/V`
- 固件已切回业务模式（非自检循环模式）。
- 前端目录约束已确定：所有前端相关内容统一放在 `front/` 下。
- 已创建联调骨架：
  - `front/gateway`：WebSocket <-> 串口网关（Node.js）
  - `front/web`：4 轴控制面板 MVP（静态页面）
- `front/web` 已接入 Three.js + STLLoader：
  - 可加载 `front/models/raw` 中 STL
  - 已按 `base/j1/j2/j3/j4` 建立结构分组并支持关节联动显示

## 3. 总体架构（落地版）

- `front/web`：Three.js（或 react-three-fiber）页面，负责 3D 展示与控制 UI。
- `front/gateway`：Node.js 串口网关，负责 WebSocket <-> 串口转发。
- `STM32 firmware`：执行舵机控制、状态读取、协议处理。

数据流：

1. 控制流：Web -> WebSocket -> Gateway -> 串口 -> STM32 -> 总线舵机。
2. 状态流：总线舵机状态 -> STM32 -> 串口 -> Gateway -> WebSocket -> Web。

## 4. 通信协议约定（两层）

### 4.1 STM32 <-> 总线舵机（已存在）

- 使用舵机原生协议。
- 帧头：`0x55 0x55`。
- 位置单位：`0~1000`（当前工程按此单位控制）。

### 4.2 Gateway <-> STM32（待实现，建议先最小可用）

先用文本行协议，便于调试与抓包：

- 下发运动：`M,<id>,<pos>,<time_ms>\n`
- 读取位置：`Q,<id>\n`
- 读取电压：`V,<id>\n`
- 返回状态：`P,<id>,<pos>\n`、`V,<id>,<mv>\n`
- 心跳/错误：`OK\n`、`ERR,<code>\n`

说明：

- 文本协议用于 MVP 快速联调；后续可平滑升级为二进制协议。
- Web 侧角度与舵机 `0~1000` 的映射由网关统一处理。

## 5. STEP 到 Web 模型处理流程

1. 在 CAD 中将机械臂拆分为 4 个关节链路和末端执行器。
2. 转换为 GLB/GLTF（建议 Blender 做中转与修正）。
3. 为每个关节设置正确 Pivot（旋转轴心必须与实物关节一致）。
4. 建立父子层级（Base -> J1 -> J2 -> J3 -> J4）。
5. 输出一份关节配置表：
   - `joint_name`
   - `axis`
   - `home`
   - `min/max`
   - `servo_id`

## 6. 近期执行计划（按当前状态重排）

### Phase A：串口链路标准化（已完成 MVP）

- 已在 STM32 增加 UART1 命令解析（支持 `M/Q/V/PING`，并兼容原协议）。
- 已在 `front/gateway` 实现串口收发与 WebSocket 转发。
- 当前可用验收口径：可通过网关向 ID=1 发送位置命令，并读取位置/电压。

### Phase B：Web 最小控制面板（2~3 天）

- 已搭建 `front/web` 基础页面（静态 MVP）。
- 已实现 4 个滑杆 + 连接状态 + 日志输出。
- 待验收：拖动滑杆驱动实机（先 ID=1，再扩 4 轴）。

### Phase C：3D 同步（2~3 天）

- 已导入 STL 并建立分组可视化。
- 已支持滑杆与回读位置驱动 3D 关节姿态变化。
- 待完成：精调 `pivot/axis`，将视觉运动与实机机械轴精确对齐。

## 7. 关键风险与规避

- 风险：舵机单位不是角度而是 `0~1000`。
  - 规避：建立统一映射函数并固化在网关层。
- 风险：方向脚切换时序导致偶发收发失败。
  - 规避：发送后等待 `TC` 再切到接收，读取前清空 RX 缓冲。
- 风险：串口高频下发造成抖动。
  - 规避：网关层限流（建议 20Hz），并做“最后指令覆盖”。
- 风险：模型轴向与实物不一致。
  - 规避：先做单关节逐轴校验，再做整机联动。

## 8. 目录约定（当前）

```text
STM32/
├─ USER/                 # STM32 业务代码
├─ gcc/                  # GCC + Makefile 构建系统
├─ front/                # 前端相关内容根目录
│  ├─ web/               # Web 前端
│  ├─ gateway/           # 串口网关（Node.js）
│  └─ models/            # 3D 模型与配置
└─ doc/                  # 硬件资料与项目文档
```

## 9. 当前下一步（立即执行）

1. 在 `front/gateway` 建立 Node.js 串口网关最小工程。
2. 定义并实现 `M/Q/P/ERR` 文本协议联调。
3. 用 ID=1 先打通 Web -> 实机 -> Web 回传闭环。
4. 闭环稳定后扩展到 4 舵机。
